{
  "name": "d-onald-org",
  "description": "Donald Hruska's personal site",
  "version": "1.1.1",
  "author": "Donald Hruska <donaldhruska@gmail.com>",
  "dependencies": {
    "fontfaceobserver": "^2.0.13",
    "next": "^4.0.1",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-ga": "^2.3.5",
    "react-photo-gallery": "^6.0.7"
  },
  "scripts": {
    "dev": "next",
    "build":
      "next build && next export -o docs && git checkout -- docs/CNAME && git checkout -- docs/.nojekyll && git checkout -- docs/google167cca62afb5f58e.html && git checkout -- docs/keybase.txt && git add docs && git commit -m \"chore: regen site\" && git push origin master",
    "start": "next start",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
