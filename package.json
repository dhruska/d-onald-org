{
  "name": "d-onald-org",
  "description": "Donald Hruska's personal site",
  "version": "1.1.1",
  "author": "Donald Hruska <donaldhruska@gmail.com>",
  "dependencies": {
    "fontfaceobserver": "^2.1.0",
    "next": "^9.3.2",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-ga": "^2.7.0",
    "react-photo-gallery": "^8.0.0"
  },
  "scripts": {
    "dev": "next",
    "build": "next build && next export -o docs && git checkout -- docs/CNAME && git checkout -- docs/.nojekyll && git checkout -- docs/google167cca62afb5f58e.html && git checkout -- docs/keybase.txt && git add docs && git commit -m \"chore: regen site\" && git push origin master",
    "start": "next start",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
